import { expect } from 'chai';
import * as auth from './auth';

it('Test logout', () => {
  // expect(typeof auth.logout()).to.equals('function');
});
// it('Test logout', () => {
//   expect(auth.changePasswordRequest()).to.be.ok;
// });
// it('Test logout', () => {
//   expect(auth.loginRequest()).to.be.ok;
// });
// it('Test logout', () => {
//   expect(auth.regRequest()).to.be.ok;
// });
